///scr_CreateEntity(animation_speed, frames, durability)

image_speed = argument0 + random(argument0 / 3);
image_index = irandom(argument1);
if (argument2 != 0) {
    durability = argument2;
}
depth = y * -1;